# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/chat-app
# Or use MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/chat-app

# Server Configuration
PORT=5001
NODE_ENV=development

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173

# JWT Secret (generate a strong random string)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# Admin Configuration
ADMIN_EMAIL=admin@example.com
ADMIN_USERNAME=admin

# Email Configuration (for password reset)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# ============================================
# REDIS CONFIGURATION (For 500K+ Users)
# ============================================
# Required for distributed rate limiting and Socket.io scaling
# Get free Redis at: https://redis.com/try-free/
# Or use Upstash: https://upstash.com/

REDIS_HOST=your-redis-host.upstash.io
REDIS_PORT=6379
REDIS_PASSWORD=your-redis-password

# Note: Redis is optional for development
# In production with multiple servers, Redis is REQUIRED for:
# - Distributed rate limiting across servers
# - Socket.io multi-server support
# - Session management
# - Caching
